spring.application.name=S

spring.datasource.url=jdbc:postgresql://localhost:5432/saleStoreDB
spring.datasource.username=postgres
spring.datasource.password=root
spring.jpa.show-sql=true

## Hibernate Properties
# The SQL dialect makes Hibernate generate better SQL for the chosen database
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.PostgreSQLDialect

# Hibernate ddl auto (create, create-drop, validate, update)
spring.jpa.hibernate.ddl-auto = update

# Configuración Jackson
#spring.jackson.serialization.write-dates-as-timestamps=false
#spring.jackson.time-zone=America/La_Paz
#
## Configuración JWT
jwt.secret-key=jaleStore2024SecretKeyForAuthenticationVeryLongAndSecureKey256Bits
jwt.expiration=86400000
jwt.refresh-token.expiration=604800000

# Configuración de logging
#logging.level.com.jaleStore.demo=DEBUG
#logging.level.org.springframework.security=DEBUG
#logging.level.org.springframework.web=INFO
server.port=9000

app.base-url=http://localhost:8084